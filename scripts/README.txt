How to use:

Put CSV file with the data to be transfered to Solr in the same folder as the script.

sudo apt install python3-pip
pip3 install pysolr
python3 csv_to_solr.py

Uses a solr core with the name "ubo_projects"